<!DOCTYPE html>
<html>

<head>
  <%- include('pages/head'); %>
</head>

<body>
          <%- include ('pages/header'); %>
<% if(typeof message!='undefined'){ %>
  <span> <%= message %> </span>
  <% } %>
          <div class="container-fluid">
           
            <% if(typeof signin!='undefined'){ %>
              <%- include ('pages/registerform'); %>
              <%}%>
            
          </div>
          <ul>
            <%if(typeof usrlst!='undefined'){%>
            <% usrlst.forEach(function(user){ %>
            <li><%=user.id %></li>  
            <li><%=user.fulname %></li>  
            <li><%=user.email %></li>  
            <li><%=user.isactive %></li>  
       
            <% }); %>
            <%}%>
          </ul>

</body>

</html>